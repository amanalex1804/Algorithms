#include<bits/stdc++.h>
using namespace std;

int main(){
        int n,s;
        cin>>n>>s;
        int A[n+5];
        for(int i=0;i<n;++i) cin>>A[i];
        
        
         
    for(int i=0;i<n;++i){
        
        int x=999,pos;
            for(int j=i;j<n;++j){
                if(A[j]<x){
                    x=A[j];
                    pos=j;
                }
            }
            swap(A[i],A[pos]);
            
    }
        for(int i=0;i<n;++i) cout<<A[i]<<" ";
        
    
    return 0;
}


/***
   
   search for minimum and swap
   
   
         *****/
